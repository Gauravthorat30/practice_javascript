let myName = new String("Hii my name is Gaurav");
let index = 8;

console.log(`The word at index ${index} is ${myName.at(index)}`);

console.log(`The character code of ${myName.charAt(index)} is ${myName.charCodeAt(index)}`);

